<%= form_for(@shop) do |f| %>
  <% if @shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
      <% @shop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :name %><%= f.text_field :name, :size => "40" %><br/>
    <%= f.label :owner %><%= f.text_field :owner, :size => "20" %><br/>
    <%= f.label :owner_id %><%= f.text_field :owner_id, :size => "20" %><br/>
    <%= f.label :address %><%= f.select :district, eval(t('.dalian_districts')) %><%= f.text_field :address, :size => "100" %><br/>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
